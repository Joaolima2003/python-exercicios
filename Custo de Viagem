#Desenvolva um programa que pergunte a distância de uma viagem em km. Calcule o preço da passagem, cobrando R$0,50 para viagens de até 200km e R$0,45 para viagem mais longas
distancia = float(input('Digite qual a distância da sua viagem'))
print('Sua viagem será de {} km'.format(distancia))
if distancia <= 200:
    valor = distancia * 0.50
    print('O valor cobrado será de R${}'.format(valor))
else:
    valor2 = distancia * 0.45
    print('O valor cobrado será de R${}'.format(valor2))
print('-=-' * 10)   
print('ATÉ MAIS')
